<script>
var chart = jui.include("chart.builder");
var data = [
    { category : "사이버 언어폭력", score : <%= @result.score_1 %> },
    { category : "사이버 따돌림", score : <%= @result.score_2 %> },
    { category : "사이버 명예훼손", score : <%= @result.score_3 %> },
    { category : "사이버 성폭력", score : <%= @result.score_4 %> },
    { category : "기타", score: <%= @result.score_5 %> }
];


$(document).ready(function() {
  chart(".results_chart", {
    padding : {
      left: 100
    },
    axis : {
      x : {
       type : "range",
       domain : [0, 100],
       step : 10,
       line : true
     },
     y : {
       type : "block",
       domain : "category",
       line : true
     },
     data : data
   },
    brush : {
     type : "bar",
     target : "score",
     display : "max",
     active : 5,
     activeEvent : "mouseover",
     animate : true,
     colors : [11],
   },
   widget : {
     type : "title",
     text : "진단 결과 점수",
     align : "start"
   }
  });
});
</script>
<div class="row jui">
  <div class="col-sm-7 col-sm-offset-1 results_chart" >
  </div>
  <div class="col-xs-10 col-xs-offset-1 col-sm-3 col-sm-offset-0 results_chart_captions">
    <div class="row">
      <div class="col-sm-12">
        <label>검사 차수</label>
        <select>
          <% @results.each do |result| %>
            <option><%= result.times %>회차</option>
          <% end %>
        </select>
      </div>
    </div>
    <div class="row results_chart_captions_contents">
      <div class="col-sm-12 results_chart_captions_contents">여기에는 차트 내용에 대한 각종 설명이 들어갈 예정<br>예를 들면 이 점수는 이런 내용이며<br>이 점수는 이런 의미이며<br>이 점수는 이러이러한 의미이다.<br>몇 점 이상이면 빠른 조치가 필요하다 등등..</div>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-xs-10 col-xs-offset-1 col-sm-10  col-sm-offset-1 results_board">
    <div class="row">
      <h1 class="col-sm-10 col-sm-offset-1 results_board_title">문제 영역</h1>
    </div>
    <div class="row">
      <ul class="col-sm-10 col-sm-offset-1 results_board_list">
        <li>사이버 언어폭력</li>
        <li>사이버 따돌림</li>
        <li>사이버 명예훼손</li>
        <li>사이버 성폭력</li>
      </ul>
    </div>
  </div>
</div>
